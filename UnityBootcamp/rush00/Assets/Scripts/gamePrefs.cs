using UnityEngine;
using System.Collections;

public class gamePrefs : MonoBehaviour {

	public bool		hasWeapon;

	// Use this for initialization
	void Start () {
//		PlayerPrefs.SetInt ("PlayerHasWeapon", 0);
//		PlayerPrefs.Save ();
	}
	
	// Update is called once per frame
	void Update () {
	}
}
